name: Flutter Release CI

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-release:
    runs-on: macos-latest

    permissions:
      contents: write
      actions: write

    env:
      FLUTTER_VERSION: '3.22.0'

    steps:
      # ===
      # 📥 Checkout
      # ===
      - name: Checkout repository
        uses: actions/checkout@v4

      # ===
      # ⚙️ Flutter
      # ===
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}

      # ===
      # 📦 Dependencias
      # ===
      - name: Install dependencies
        run: flutter pub get

      # ===
      # 🔐 ANDROID KEYSTORE
      # ===
      - name: Decode keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > android/app/upload-keystore.jks

      - name: Create key.properties
        run: |
          echo "storePassword=${{ secrets.ANDROID_STORE_PASSWORD }}" > android/key.properties
          echo "keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}" >> android/key.properties
          echo "keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}" >> android/key.properties
          echo "storeFile=upload-keystore.jks" >> android/key.properties

      # ===
      # 🧹 CLEAN
      # ===
      - name: Flutter clean
        run: flutter clean

      # ===
      # 🤖 ANDROID BUILD
      # ===
      - name: Build APK (Release)
        run: flutter build apk --release

      - name: Build AAB (Release)
        run: flutter build appbundle --release

      # ===
      # 🍏 VALIDAR SECRETS iOS
      # ===
      - name: Validate iOS secrets
        id: ios_check
        run: |
          if [ -z "${{ secrets.IOS_CERTIFICATE_BASE64 }}" ] || \
             [ -z "${{ secrets.IOS_PROVISIONING_PROFILE_BASE64 }}" ] || \
             [ -z "${{ secrets.IOS_CERTIFICATE_PASSWORD }}" ] || \
             [ -z "${{ secrets.KEYCHAIN_PASSWORD }}" ]; then
            echo "ios_ready=false" >> $GITHUB_OUTPUT
            echo "⚠️ iOS build SKIPPED: faltan secrets"
          else
            echo "ios_ready=true" >> $GITHUB_OUTPUT
          fi

      # ===
      # 🍏 CONFIGURAR iOS (SOLO SI HAY SECRETS)
      # ===
      - name: Decode iOS certificate & profile
        if: steps.ios_check.outputs.ios_ready == 'true'
        run: |
          echo "${{ secrets.IOS_CERTIFICATE_BASE64 }}" | base64 --decode > certificate.p12
          echo "${{ secrets.IOS_PROVISIONING_PROFILE_BASE64 }}" | base64 --decode > profile.mobileprovision

      - name: Setup keychain
        if: steps.ios_check.outputs.ios_ready == 'true'
        run: |
          security create-keychain -p "${{ secrets.KEYCHAIN_PASSWORD }}" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "${{ secrets.KEYCHAIN_PASSWORD }}" build.keychain
          security import certificate.p12 -k build.keychain -P "${{ secrets.IOS_CERTIFICATE_PASSWORD }}" -T /usr/bin/codesign

      - name: Install provisioning profile
        if: steps.ios_check.outputs.ios_ready == 'true'
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/

      # ===
      # 🍏 BUILD IPA
      # ===
      - name: Build IPA
        if: steps.ios_check.outputs.ios_ready == 'true'
        run: flutter build ipa --release

      # ===
      # 📤 ARTIFACTS
      # ===
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: Upload AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-aab
          path: build/app/outputs/bundle/release/app-release.aab

      - name: Upload IPA Artifact
        if: steps.ios_check.outputs.ios_ready == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: app-release-ipa
          path: build/ios/ipa/*.ipa

      # ===
      # 🚀 RELEASE
      # ===
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ github.ref_name }}
          name: Flutter Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          artifacts: |
            build/app/outputs/flutter-apk/app-release.apk
            build/app/outputs/bundle/release/app-release.aab
            build/ios/ipa/*.ipa
          token: ${{ secrets.GITHUB_TOKEN }}